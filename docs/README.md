# PPI 项目报告（docs 唯一文档）

> 本文件是 `docs/` 目录的唯一报告，替代原 README 索引、TRAINING_OPTIMIZATION、
> EXPERIMENT_SUMMARY 与两份历史实验记录。
>
> 背景：训练/验证/测试的采样知识图谱（KG）由 split-local 图改为**全数据集全图**
> （见下文「知识图谱设计」），原文档中所有以 split-local KG 为前提的表述与实验
> 结论不再直接成立，故压缩为单份报告。本文同时是当前代码的唯一文档事实源。

## 1. 项目概述

- 7 类 PPI 多标签预测（reaction / binding / ptmod / activation / inhibition /
  catalysis / expression），标签为按无向蛋白对 OR 聚合的 multi-hot 向量。
- 蛋白特征为预计算 ESM-2 3B（`esm2_t36_3B_UR50D`）平均池化 embedding，
  `[M, 2560]` bfloat16（`{name}_tensor.pt`，`tensor[i]` = Protein Index `i`）。
- 数据：SHS27k（1690 蛋白 / 7624 PPI）、SHS148k（5189 / 44488）、
  STRING（15335 / 593397）；`ppi_list` 中每个无向 PPI 对恰好出现一次（方向任意，
  不存在双向重复），`ppi_list` 行号 = PPI Index。
- 管线：split 选出目标 PPI 对 → 全图 KG 上的 REINFORCE 子图 Sampler → GAT
  Predictor（多图 batch 编码 → `h_u+h_v`、`|h_u−h_v|`、图均值 → 7 维 logits）。

## 2. 知识图谱设计（当前）

- **训练、验证、测试三阶段共用同一张全图**：节点 = 全部蛋白，边 = 全部 PPI 对
  （双向），特征 = 全部 ESM embedding，proxy 候选池 = 全部蛋白。由
  `PPIGraph.build_full_graph(undirected=True)` 构建，`train_shs27k.py` 中只构建
  一次并全程共享（含邻接表 `full_adjacency`）。
- **split 只做两件事**：① 决定各阶段的目标 PPI 对（`get_ppi_indices` 提取
  train/val/test targets 与 labels）；② 提供训练节点集合，用于测试集
  BS/ES/NS 可见性分组（`train_node_index`，来自 `build_graph("train")`）。
- 可选 `--use-edge-relations` 将 train split 边的 7 维 multi-hot relation 输入
  Predictor GAT；val/test 边仍保留为无标签拓扑，但 relation 全零，虚拟 proxy
  边与 self-loop 也为全零。该开关默认关闭，以保持历史实验可复现。
- 可选 `--use-sampler-edge-relations` 独立将 train-edge relation 输入 RL 动作
  打分：候选节点连接当前已选子图的关系按逐维 max/OR 聚合、投影后加到 candidate
  表示。目标边先从安全邻接删除，held-out 与 proxy 边 relation 为零。
- **不变量（必须保持）**：
  - 目标边 `(u,v)` 与 `(v,u)` 在每次采样前从安全邻接中排除（
    `_TargetSafeAdjacency` 惰性视图，共享邻接不被修改），且不进入 `G_0` 与
    step graph；
  - `G_0`（`baseline_graph`）只含 `u`、`v` 与必要虚拟 proxy（安全邻接为空的
    目标才选 proxy，按 embedding 余弦相似度，两目标可共享）；`G_0` 保留选中
    节点间的全部安全诱导边；
  - 候选限制在距 `G_0` 种子不超过 `k_hops`（默认 1）的安全区域内；`max_steps`
    （默认 10）只限制动作次数，无 STOP 动作；
  - Predictor 训练与评估均使用 `final_graph`（无动作时即 `G_0`）；
  - 默认模式的推理特征只含 ESM embedding 与无标签拓扑；relation-aware 模式
    额外读取 train split 边标签。val/test relation 从不进入关系查询表，且当前
    查询目标边在 edge feature 物化前已删除；
  - 奖励/return-to-go/advantage 保持 FP32；F1 阈值固定 0.5。
- 说明：`build_graph(split_name)` 的 split-local 构建保留（节点集合查询、测试、
  外部使用），但训练入口不再以它作为 KG。

## 3. 训练协议（AlternatingTrainer）

每 batch 交替两次更新：

1. **Sampler 更新**（Predictor 冻结，stochastic 轨迹）：对每条轨迹计算
   `G_0` 与所有 step graph 的 BCE loss，增量奖励 `r_t = L(G_{t-1}) − L(G_t)`
   （第一步以 `G_0` loss 为前项；无子图大小/Δn 惩罚），按 `G_t = r_t + γG_{t+1}`
   计算 return-to-go；无学习 baseline，advantage 即 detached RTG，batch 内
   标准化 `Â = (A − mean)/max(std, 1e-8)` 后算
   `L_policy = −Σ log π(a_t|s_t)·stopgrad(Â)` 更新 Sampler。
2. **Predictor 更新**（Sampler 冻结，greedy 轨迹）：只用每条轨迹的
   `final_graph` 做 BCE with logits 更新。

动作打分：state/candidate 独立投影，拼接映射回 hidden_dim 并加回投影 state
（残差）→ `Linear(d→d//2) → LayerNorm → Tanh → Linear(d//2→1)` → softmax；
训练记录 Categorical `log_prob`，评估/更新用 greedy argmax。
启用 `--use-sampler-edge-relations` 时，candidate 投影额外融合候选到已选子图的
7 维关系聚合；该开关仅适用于 `--sampler rl`，可与 Predictor 开关独立消融。

超参默认：`--epochs 10 --batch-size 32 --eval-batch-size 64 --hidden-dim 256
--max-steps 10 --k-hops 1 --gnn-layers 2 --heads 4 --dropout 0.1
--sampler-lr 1e-4 --predictor-lr 1e-3 --reinforce-gamma 1.0`；relation-aware
Predictor 与 Sampler 分别通过 `--use-edge-relations` 和
`--use-sampler-edge-relations` 显式开启。
历史推荐配置（split-local 时代，见 §6）：`h128 / γ0.9 / 20ep / ms5`。

## 4. 评估协议

- 训练期间**只评估验证集**（每 epoch 一次）；按验证 Macro-AUC 保存最佳
  Sampler/Predictor 状态（`--checkpoint-dir` 指定时落盘，否则内存保存）；
  训练结束后**只在最佳 checkpoint 上测试一次**。
- 指标：Macro/Micro ROC-AUC 与固定阈值 0.5 的 Macro/Micro F1；常数类（单值）
  子集 AUC 报 `None`（与空分组同约定），不参与最佳选择。
- 测试集按两端节点相对训练节点集合的可见性分 BS（两端均训练可见）/ ES（单端）
  / NS（均不可见）；空分组返回 `count=0` 与 `None`。
- 可见性分组**仍按 train split 节点集**计算（全图下"图中节点"无区分度）。

## 5. 数据与性能

### 5.1 规模（全图即有向边数）

| 数据集 | 蛋白 | PPI 对 | 全图有向边 | 全图 node_feat(fp32) |
|---|---:|---:|---:|---:|
| SHS27k | 1,690 | 7,624 | 15,248 | ~17 MB |
| SHS148k | 5,189 | 44,488 | 88,976 | ~53 MB |
| STRING | 15,335 | 593,397 | 1,186,794 | ~157 MB |

每个蛋白都出现在 ≥1 条 PPI 中，因此全图 `node_index` 覆盖全部蛋白且局部 id =
全局 id（实现仍用 `unique()` 通用处理）。

### 5.2 数据喂入（结论）

训练循环用 `torch.randperm` + 批量 fancy-index gather 直接从预载 tensor 取数
（`train_targets[batch_indices]` / `train_labels[batch_indices]`），这是该负载
（数据完全驻留内存/显存、无 I/O、无逐样本预处理）下的最优形态；历史对比中
DataLoader 路径慢 1–3 个数量级（`num_workers>0` 还会每 epoch 重 spawn +
pickle 整个 PPIGraph），且 CUDA 下逐样本 `int()` 引入 D2H 同步。
`get_dataloader`/`_PPIDataset` 已无任何调用方，2026-09-04 作为死代码删除，
训练/评估均不经过 DataLoader。

### 5.3 邻接与计算

- 全图邻接（不可变 `tuple[frozenset]`）**一次性构建**，训练 batch、每 epoch
  验证、最终测试共享（`train_shs27k.py` 与 `evaluate(..., adjacency=...)`）；
  目标边由惰性视图逐 target 排除，无逐 target 复制。
- 采样区域/候选池随全图度数扩大（STRING 的 hub 目标 1-hop 区域可能很大），
  受 `k_hops`/`max_steps` 约束；训练时间主要由 Sampler 轨迹生成、Sampler 更新
  阶段对多个 step graph 的 Predictor 前向、验证推理构成。
- 仍有效的优化方向（与 split-local 无关的部分）：P1 缓存归一化 embedding 与
  candidate 投影、增量维护 selected embedding 均值、减少/合并 Predictor 重复
  forward；P2 评估 `torch.inference_mode()`、batch size 扫描、Predictor BF16/
  AMP；P3 STRING 用 CSR tensor 邻接 + tensor frontier（全图下收益更大）。
  Profiling 需分别统计 sampler_g0_build / sampler_action_score /
  sampler_graph_build / predictor_baseline / predictor_step_graphs /
  predictor_update / validation / test_evaluation；CUDA 计时前
  `torch.cuda.synchronize()`。

## 6. 历史实验记录（split-local KG 时代，仅作配置起点）

> **失效声明**：以下结论全部产生于 KG 为 split-local 图的旧代码（全图改造
> 前），数值与排名不可直接迁移到全图 KG，仅保留为超参/协议起点与仓库历史。
> 全图改造后需重新验证。

- 56 次运行（3-seed 配对为主）关键结论：
  - 最优配置（旧）：`res-score + hidden 128 + gamma 0.9 + 20 epochs +
    max_steps 5`（SHS27k bfs test MacF1 0.5239）；res-score 打分头为当前代码
    默认架构（配对 MacF1 +0.050±0.052，方差更小）；value baseline 删除无显著
    指标差异（省时 ~13%），当前代码即无 value_head 版本。
  - 划分效应是最大"杠杆"且**跨划分不可混比**：dfs 划分任务系统性更易
    （SHS27k MacF1 0.601 vs bfs 0.515，测试可见性 79.5% vs 70.1%）；新随机
    bfs 划分波动即达 MacF1 ~0.03-0.06；SHS148k random 指标虚高（test BS
    96.5%，预测退化为已见节点模式匹配）；SHS148k bfs 训练崩溃为划分数据属性
    （train 稠密核心 vs val 稀疏外围），与 RL 机制无关。
  - sampler/RL 在 dfs 划分上无正向贡献（ms0 即达全配水平：SHS27k dfs MacF1
    0.6295±0.0172 vs ms5 0.6006，3/3 正；SHS148k dfs 持平但省时 ~14×）；
    G0-only predictor 为旧代码确认的最优形态。
  - 长训练：40ep 为成本-收益甜点（SHS148k dfs ms0 valMacAUC 0.8557±0.0004），
    h256 vs h128 无显著差异；k_hops 2 纯净配对无显著收益，默认 k1。
- 纪律（仍然适用）：指标方差大（MacF1 0.36~0.52），结论须多 seed 配对均值±std；
  默认模式推理特征不含任何标签信息（relation-aware 模式仅在显式开启
  `--use-edge-relations`/`--use-sampler-edge-relations` 时读取 train 边标签，
  val/test 与目标边标签从不进入，见 §2）；报告结果须标注数据集与 split。
- 建议命令（配置起点，全图 KG 自动生效）：

```bash
python -m src.train_shs27k \
  --dataset SHS27k --split bfs --device cuda \
  --epochs 20 --hidden-dim 128 --max-steps 5 --reinforce-gamma 0.9 \
  --seed 42 --output /tmp/ppi_best.json
```

## 7. 全图 KG 实验记录（SHS27k bfs 3-seed 基线，2025-08-24）

配置与 §6 旧基线同款（`ms5/h128/g0.9/20ep`，seed 42/111/123，CPU），唯一差别是
KG 由 split-local 改为全图（提交 `23794a8` 之后）。原始 JSON：
`/tmp/ppi_fullkg_s{42,111,123}.json`。

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 1221 | 9 | 0.7717 | 0.7817 | 0.7981 | 0.5789 | 0.5974 |
| 111 | 1148 | 6 | 0.7574 | 0.7539 | 0.7739 | 0.4876 | 0.5537 |
| 123 | 1037 | 14 | 0.7867 | 0.7917 | 0.8183 | 0.5879 | 0.6285 |
| 均值±std | 1136±93 | — | 0.7719±0.0146 | 0.7758±0.0196 | 0.7968±0.0223 | 0.5515±0.0555 | 0.5932±0.0376 |

与 split-local 同配置 3-seed 配对 diff（全图 − 旧，五项指标全为 3/3 正）：

| 指标 | 配对 diff | 旧基线均值 |
|---|---|---:|
| MacAUC | +0.0248±0.0120 | 0.7509±0.0075 |
| MicAUC | +0.0226±0.0124 | 0.7742±0.0161 |
| MacF1 | +0.0367±0.0266 | 0.5148±0.0238 |
| MicF1 | +0.0510±0.0144 | 0.5422±0.0379 |
| valMacAUC | +0.0155±0.0056 | 0.7564±0.0116 |

- 速度：epoch ~52-65 s，总时长 1037-1221 s（s42 1221 s vs 旧 1179 s 持平）；
  全图 KG 在 SHS27k 上无速度损失（邻域扩大被小图规模抵消）。
- s111 仍为最弱 seed（MacF1 0.4876 vs 旧 0.4811，全图下相对差距略放大），
  MacF1 的 std 由 0.024 增至 0.055，方差主要来自 s111。
- 测试可见性分组计数不变（BS 0 / ES 1078 / NS 460），符合预期（分组与 KG 无关）。
- 单配置 3-seed 配对，方向一致但样本小，结论待更多 seed 验证。

## 8. 全图 KG 实验记录（SHS27k bfs 消融：ms0/ms10/static/random-subset，2025-08-25）

### 8.0 ms0（`--max-steps 0`，G0-only）

配置同 §7（`h128/g0.9/20ep`，seed 42/111/123，CPU），仅 `--max-steps 0`：
轨迹无动作、`final_graph` ≡ `G_0`，Sampler 不更新（`sampler_loss`/`mean_reward`
恒为 0），Predictor 只在 G0 上训练。3 seed 并行（每进程 3 线程，总墙钟 ~87 s）。
原始 JSON：`/tmp/ppi_ms0_s{42,111,123}.json`。

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 87 | 14 | 0.7436 | 0.7360 | 0.7427 | 0.4935 | 0.5189 |
| 111 | 87 | 12 | 0.7555 | 0.7574 | 0.7709 | 0.4943 | 0.5510 |
| 123 | 87 | 20 | 0.7595 | 0.7558 | 0.7801 | 0.5256 | 0.5789 |
| 均值±std | 87.0±0.4 | — | 0.7529±0.0083 | 0.7497±0.0120 | 0.7646±0.0195 | 0.5045±0.0183 | 0.5496±0.0300 |

与 §7 ms5 基线配对 diff（ms0 − ms5）：

| 指标 | 配对 diff | 正向 seed 数 |
|---|---|---:|
| valMacAUC | −0.0191±0.0149 | 0/3 |
| MacAUC | −0.0260±0.0261 | 1/3 |
| MicAUC | −0.0322±0.0267 | 0/3 |
| MacF1 | −0.0470±0.0479 | 1/3 |
| MicF1 | −0.0436±0.0382 | 0/3 |
| 秒 | −1048±92 | — |

- 速度：epoch ~4.3 s（3 线程并行），总 87 s vs ms5 1136 s，**约 13×**（省去
  Sampler 更新与 step-graph 前向，这两者是 ms5 的训练耗时主体）。
- 指标：ms0 五项均值全面低于 ms5（val/Mic 0/3 正、Mac 1/3 正），差距集中在
  s42/s123（MacF1 −0.085/−0.062）；s111 例外（MacAUC +0.0035、MacF1 +0.0067，
  基本持平）。**全图 KG + SHS27k bfs 下采样动作（ms5）有正向贡献**，与 §6
  split-local 时代"dfs 上 ms0 最优"不冲突（划分/KG 均不同）；但差距与 seed 间
  方差同量级（MacF1 差 ~0.05 vs std ~0.05），仅 3-seed，待扩 seed 确认。
- bestEp 后移（12-20 vs ms5 的 6-14）：无采样时验证曲线峰值更晚。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

### 8.1 ms10（`--max-steps 10`）

同批补跑（同配置 `h128/g0.9/20ep`，seed 42/111/123，CPU），3 seed 并行
（每进程 3 线程）。原始 JSON：`/tmp/ppi_ms10_s{42,111,123}.json`。

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 2929 | 8 | 0.7522 | 0.7560 | 0.7820 | 0.5499 | 0.5751 |
| 111 | 3024 | 15 | 0.7728 | 0.7777 | 0.7745 | 0.5242 | 0.5434 |
| 123 | 2897 | 8 | 0.7546 | 0.7667 | 0.7929 | 0.5534 | 0.6097 |
| 均值±std | 2950±66 | — | 0.7599±0.0113 | 0.7668±0.0109 | 0.7831±0.0092 | 0.5425±0.0160 | 0.5761±0.0332 |

配对 diff（括号内为正向 seed 数）：

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| ms10 − ms5 | −0.0121±0.0246（1/3） | −0.0090±0.0284（1/3） | −0.0136±0.0132（1/3） | −0.0090±0.0395（1/3） | −0.0171±0.0062（0/3） | +1815±93 |
| ms10 − ms0 | +0.0070±0.0112（2/3） | +0.0171±0.0054（3/3） | +0.0186±0.0185（3/3） | +0.0380±0.0160（3/3） | +0.0265±0.0321（2/3） | +2863±66 |

- 排序（均值）：五项指标均为 **ms5 > ms10 > ms0**；ms10−ms5 差距小且在 std
  内（仅 1/3 正），ms10−ms0 差距大（MacAUC/MicAUC/MacF1 3/3 正）——采样动作的
  贡献稳健，最优步数在 ms5 附近（5-10 间或为平台期），3-seed 不足以区分
  ms5/ms10。
- s111（ms5 最弱 seed）在 ms10 下明显改善（MacAUC +0.0238、MacF1 +0.0366），
  MacF1 的 seed 间方差由 ms5 的 0.055 降至 0.016——更长步数上限对弱 seed 更稳。
- 速度：ms10 epoch ~147 s（3 线程并行）；与 ms5（~55 s/epoch @ 8 线程）按线程
  数折算后单 epoch 计算量同量级——`k_hops=1` 下 frontier 常在 5 步内耗尽，10
  步上限很少真正触达；总墙钟 2950 s 主要是"并行 3 线程 vs 顺序 8 线程"的
  差异，不是纯 max_steps 效应。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

### 8.2 static（`--sampler static`：G0 + 全部 1-hop 安全邻居，不可学习）

配置同 §7（`h128/g0.9/20ep`，seed 42/111/123，CPU），仅换用
`StaticNeighborhoodSampler`（提交 `425ef43` 引入）：图 = G0 种子（u/v/必要
proxy）的全部安全 `k_hops=1` 区域诱导子图，无动作、零参数，训练只更新
Predictor。3 seed 并行（每进程 3 线程）。原始 JSON：
`/tmp/ppi_static_s{42,111,123}.json`。

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 338 | 18 | 0.8022 | 0.7995 | 0.8189 | 0.5961 | 0.6210 |
| 111 | 336 | 11 | 0.8068 | 0.8085 | 0.8233 | 0.6063 | 0.6280 |
| 123 | 336 | 17 | 0.7992 | 0.8061 | 0.8187 | 0.5819 | 0.6064 |
| 均值±std | 337±1 | — | 0.8027±0.0038 | 0.8047±0.0046 | 0.8203±0.0026 | 0.5947±0.0123 | 0.6185±0.0110 |

配对 diff（括号内为正向 seed 数）：

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| static − ms5 | +0.0308±0.0185（3/3） | +0.0289±0.0223（3/3） | +0.0235±0.0246（3/3） | +0.0433±0.0663（2/3） | +0.0253±0.0482（2/3） | −799±92 |
| static − ms0 | +0.0498±0.0095（3/3） | +0.0550±0.0074（3/3） | +0.0557±0.0190（3/3） | +0.0903±0.0298（3/3） | +0.0689±0.0380（3/3） | +250±1 |

图规模（train 目标 4562 个实测）：静态图节点 mean 45.6 / median 41 / max 156，
无向边 mean 238 / median 131 / max 1451（hub 目标区域大）；对照 G0 恒为 2 节点
（全图下安全邻接非空、无 proxy）。

- **结论：RL 选取在 SHS27k bfs 全图 KG 上无正向作用，且显著劣于全 1-hop 邻域**：
  static 的 AUC 三项 3/3 正、F1 2/3 正（MacAUC 均值 +0.029、valMacAUC +0.031、
  MacF1 +0.043）；s111（ms5 最弱 seed）提升最大（MacAUC +0.055、MacF1 +0.119）；
  方差大幅下降（MacAUC std 0.0196→0.0046，MacF1 std 0.0555→0.0123）。
- 机制解释：`k_hops=1` 候选区域平均 ~46 节点（RL 每轨迹最多选 5-10 个），
  static 全取；GAT Predictor 从完整邻域获得更多上下文，学习式截断反而丢信息，
  在该数据上未见"降噪"收益。
- 速度：static 每 epoch ~16.7 s（3 线程），总 337 s ≈ ms5 的 1/3.4（省去 sampler
  更新与 step-graph 前向；Predictor 前向图更大但总量更小）。
- 三档排序（均值，五项指标）：**static > ms5 > ms0**——采样器在 SHS27k bfs
  全图 KG 上的最优形态是"全取 1-hop 邻域"，RL 选取与 G0-only 均非最优。
  与 §6 split-local 时代"G0-only 最优（dfs）"不冲突（划分/KG 均不同），但本次
  结果提示：该数据下 RL 选取机制可被静态邻域替代。
- 若需进一步定位"选取策略"与"上下文大小"的效应，可加"随机选取同等规模子集"
  对照（后续可选）。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

### 8.3 random-subset 诊断（与 RL 同规模的随机选取，2025-08-25）

配置同 §7（`h128/g0.9/20ep`，seed 42/111/123，CPU），仅换用
`RandomSubsetSampler`（提交 `0c53d2d` 引入）：从 1-hop 区域**均匀随机**取与 RL
同规模的节点子集（恒含 u/v）+ 诱导边，零参数、仅训练 Predictor。规模对齐依据：
实测 ms5 每轨迹平均 4.96 步 → RL 最终图规模均值 6.96（几乎恒为 7 节点），故取
`--random-subset-min-size 6 --random-subset-max-size 7`（均值 6.5）。原始 JSON：
`/tmp/ppi_rnd_s{42,111,123}.json`。

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 99 | 14 | 0.7595 | 0.7640 | 0.7893 | 0.5336 | 0.5685 |
| 111 | 102 | 6 | 0.7518 | 0.7550 | 0.7843 | 0.4908 | 0.5461 |
| 123 | 103 | 20 | 0.7625 | 0.7569 | 0.7770 | 0.5196 | 0.5494 |
| 均值±std | 101±2 | — | 0.7579±0.0055 | 0.7586±0.0048 | 0.7835±0.0062 | 0.5146±0.0219 | 0.5547±0.0121 |

配对 diff（括号内为正向 seed 数）：

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| rnd − ms5 | −0.0140±0.0094（0/3） | −0.0172±0.0179（1/3） | −0.0132±0.0261（1/3） | −0.0368±0.0365（1/3） | −0.0385±0.0367（0/3） | −1034±95 |
| rnd − ms0 | +0.0051±0.0100（2/3） | +0.0089±0.0167（2/3） | +0.0190±0.0253（2/3） | +0.0102±0.0260（1/3） | +0.0051±0.0405（1/3） | +14±3 |
| rnd − static | −0.0448±0.0094（0/3） | −0.0461±0.0094（0/3） | −0.0367±0.0064（0/3） | −0.0801±0.0307（0/3） | −0.0638±0.0158（0/3） | −236±4 |

- **RL（ms5）与同规模随机基本持平**：配对 diff 方向不齐（MacAUC/MacF1 各 1/3
  正、MicF1 0/3 正），均值差距（0.017-0.039）与 seed 方差同量级，且 s111 上 rnd
  反超（MacAUC 0.7550 vs ms5 0.7539）→ **策略相对随机选取的增益微弱且不稳健**
  （策略没学到超越随机的东西）。
- **上下文量是主导因素**：rnd（~6.5 节点）− static（~46 节点）全 0/3 正、差距
  0.046-0.080，远大于 rnd−ms5；rnd 与 ms0（2 节点）几乎持平（MacF1 +0.010、
  1/3 正）→ 6-7 节点上下文对预测几乎无增益，GAT 需要接近全邻域的上下文量。
- **综合判读（R1 结论）**：SHS27k bfs 全图 KG 上 RL 与各对照的差距**主要由
  上下文量解释**；"RL 选取"相对随机的增益 ≤ 噪声水平。据此，"提升 RL 性能"
  的方向收敛为**扩大上下文预算**（k_hops=2 + 更多步，或"全邻域出发剪枝"式
  重构 + Δn 惩罚），而非打磨选取策略（状态/打分增强优先级下调）。
- 速度：rnd 总 101 s（epoch ~5 s @ 3 线程），≈ ms0 量级、≈ ms5 的 1/11。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

## 9. 全图 KG 实验记录（SHS27k dfs 消融：ms0/ms5/static/random-subset，2025-08-26）

在 §7/§8 同款配置（`h128/g0.9/20ep`，seed 42/111/123，CPU，每进程 3 线程、
同条件 3 seed 并行）下仅换 `--split dfs`，复刻 bfs 消融套件核心 4 条件
（按计划不跑 ms10），检验 §8 结论是否跨划分成立，并重访 §6 旧结论
"split-local 时代 dfs 上 ms0 最优"。dfs 划分规模：train 4571 / val 1524 /
test 1529（bfs：train 4562 / test 1538）。原始 JSON：
`/tmp/ppi_dfs_{ms0,ms5,static,rnd}_s{42,111,123}.json`。

### 9.1 结果表（test 指标，best checkpoint by valMacAUC）

**ms5（RL，`--max-steps 5`）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---|---:|---:|---:|---:|---:|---:|
| 42 | 1399 | 15 | 0.8515 | 0.8496 | 0.8771 | 0.6333 | 0.7008 |
| 111 | 1498 | 17 | 0.8484 | 0.8459 | 0.8713 | 0.6395 | 0.6866 |
| 123 | 1472 | 18 | 0.8570 | 0.8525 | 0.8706 | 0.6333 | 0.6934 |
| 均值±std | 1456±51 | — | 0.8523±0.0044 | 0.8493±0.0033 | 0.8730±0.0036 | 0.6354±0.0035 | 0.6936±0.0071 |

**ms0（`--max-steps 0`，G0-only）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---|---:|---:|---:|---:|---:|---:|
| 42 | 82 | 20 | 0.8368 | 0.8375 | 0.8526 | 0.6169 | 0.6751 |
| 111 | 82 | 17 | 0.8402 | 0.8420 | 0.8620 | 0.6209 | 0.6778 |
| 123 | 82 | 20 | 0.8414 | 0.8318 | 0.8588 | 0.6144 | 0.6878 |
| 均值±std | 82±0.3 | — | 0.8395±0.0024 | 0.8371±0.0051 | 0.8578±0.0048 | 0.6174±0.0033 | 0.6802±0.0067 |

**static（`--sampler static`，G0 + 全部 1-hop 安全邻居）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---|---:|---:|---:|---:|---:|---:|
| 42 | 389 | 18 | 0.8678 | 0.8585 | 0.8783 | 0.6664 | 0.7125 |
| 111 | 384 | 20 | 0.8701 | 0.8722 | 0.8934 | 0.6764 | 0.7295 |
| 123 | 387 | 11 | 0.8751 | 0.8722 | 0.8899 | 0.6714 | 0.7150 |
| 均值±std | 386±2 | — | 0.8710±0.0037 | 0.8676±0.0079 | 0.8872±0.0079 | 0.6714±0.0050 | 0.7190±0.0092 |

**rnd（`--sampler random-subset --random-subset-min-size 6 --max-size 7`，
规模对齐依据见 9.3）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---|---:|---:|---:|---:|---:|---:|
| 42 | 100 | 16 | 0.8392 | 0.8312 | 0.8478 | 0.6229 | 0.6550 |
| 111 | 101 | 20 | 0.8506 | 0.8446 | 0.8662 | 0.6405 | 0.6911 |
| 123 | 100 | 15 | 0.8440 | 0.8404 | 0.8551 | 0.6187 | 0.6661 |
| 均值±std | 101±1 | — | 0.8446±0.0057 | 0.8387±0.0069 | 0.8563±0.0093 | 0.6273±0.0116 | 0.6707±0.0185 |

### 9.2 dfs 内配对 diff（括号内为正向 seed 数）

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| ms5 − ms0 | +0.0128±0.0041（3/3） | +0.0122±0.0084（3/3） | +0.0152±0.0082（3/3） | +0.0179±0.0013（3/3） | +0.0134±0.0108（3/3） | +1374±52 |
| static − ms5 | +0.0187±0.0027（3/3） | +0.0183±0.0088（3/3） | +0.0142±0.0113（3/3） | +0.0360±0.0026（3/3） | +0.0254±0.0159（3/3） | −1070±54 |
| rnd − ms5 | −0.0077±0.0086（1/3） | −0.0106±0.0087（0/3） | −0.0167±0.0121（0/3） | −0.0080±0.0081（1/3） | −0.0229±0.0254（1/3） | −1356±51 |
| rnd − ms0 | +0.0051±0.0045（3/3） | +0.0017±0.0075（2/3） | −0.0014±0.0049（1/3） | +0.0099±0.0084（3/3） | −0.0095±0.0197（1/3） | +19±0.5 |
| rnd − static | −0.0264±0.0061（0/3） | −0.0289±0.0025（0/3） | −0.0309±0.0039（0/3） | −0.0440±0.0084（0/3） | −0.0483±0.0096（0/3） | −286±3 |

### 9.3 诊断

- **RL 规模（dfs）**：每轨迹平均 4.96 步 → 最终图规模均值 6.96，与 bfs 实测
  完全一致 → rnd 沿用 6..7（均值 6.5）对齐有效。
- **static 区域规模（dfs train 4571 目标实测）**：节点 mean 49.5 / median 48 /
  max 184，无向边 mean 315 / median 165 / max 1636；对照 bfs（45.6/41/156
  节点、238/131/1451 边）——全图相同，差异来自 dfs train 目标集合组成
  （邻域略大，也解释了 ms5 的 step 图前向更贵）。
- **测试可见性（dfs）**：BS 0 / ES 1215 / NS 314（bfs：0/1078/460）→ NS 占比
  20.5% vs 29.9%，dfs 测试对更易见。

### 9.4 跨划分配对（dfs − bfs，同 seed，全部 3/3 正向）

| 条件 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| ms5 | +0.0804±0.0104 | +0.0735±0.0163 | +0.0762±0.0227 | +0.0839±0.0590 | +0.1004±0.0341 | +321±131 |
| ms0 | +0.0866±0.0059 | +0.0873±0.0130 | +0.0932±0.0157 | +0.1129±0.0209 | +0.1306±0.0239 | −5±0.5 |
| static | +0.0683±0.0067 | +0.0629±0.0036 | +0.0670±0.0065 | +0.0766±0.0112 | +0.1005±0.0086 | +50±2 |
| rnd | +0.0867±0.0106 | +0.0801±0.0116 | +0.0728±0.0126 | +0.1127±0.0324 | +0.1160±0.0293 | −0.7±2.2 |

### 9.5 判读

- **三档排序（均值）static > ms5 > rnd ≈ ms0 在 dfs 下成立**（MacAUC/MicAUC 上
  rnd 与 ms0 持平、MacF1 上 rnd 略高）；static−ms5 五项 3/3 正、static−rnd 全
  0/3 正（差距 0.029-0.048）→ **上下文量仍是主导因素**，与 bfs 结论一致。
- **采样贡献在 dfs 上更稳健**：ms5−ms0 五项 3/3 正且 seed 间高度一致（MacF1
  +0.0179±0.0013、MacAUC +0.0122±0.0084）——**§6 旧结论"dfs 上 ms0 最优
  （3/3 正）"在全图 KG 下不成立（反转）**；现两个划分均为 ms5 > ms0，采样
  动作的贡献在 dfs 上比 bfs 更整齐（bfs MacF1 差 ~0.047±0.048 vs dfs
  ~0.018±0.001）。
- **RL vs 同规模随机（dfs）**：rnd−ms5 五项均值全负、AUC 0/3、F1 1/3——方向
  提示 dfs 下 RL 略优于随机（与 bfs 的"≈持平、1/3 正"不同），但差距
  （0.008-0.023）与 seed std 同量级、3-seed 不足以确证；保守判读：策略增益
  至多微弱，未改变"上下文量主导"的主结论。
- **dfs 系统性更易**：四条件五项指标全部 3/3 高于 bfs（MacAUC +0.06~0.09、
  MacF1 +0.05~0.15），与 NS 占比低（20.5% vs 29.9%）一致，与 §6 旧观察
  "dfs 更易"吻合（跨划分不可混比，仅作方向说明）。
- 速度：ms5 dfs 1456 s vs bfs 1136 s（+28%，dfs train 目标 1-hop 区域更大 →
  step 图前向更贵）；static 386 s、rnd 101 s、ms0 82 s（与 bfs 同量级）。
- 综合：R1"上下文量主导、策略相对随机增益 ≤ 噪声"结论在 dfs 下**成立且
  更强**（static 依旧最优、差距更大且更齐）；RL 改进首选项仍是**扩大上下文
  预算**（P1a：k_hops=2 + 更多步，或 P1b：全邻域出发剪枝 + Δn 惩罚）；若想
  确证 dfs 下 RL 的微弱正增益（rnd−ms5 全负方向），需扩 seed。

## 10. 全图 KG 实验记录（SHS27k bfs ms20：RL vs random-subset，2025-08-27）

在 §7/§8 同款配置（`h128/g0.9/20ep`，seed 42/111/123，CPU，每进程 3 线程、
同条件 3 seed 并行）下仅换 `--max-steps 20`，对比 RL（ms20）与
random-subset（`--random-subset-min-size 19 --max-size 21`，规模对齐依据见
10.3）。检验 §8.3 "RL≈随机、上下文量主导"结论在更大上下文预算下是否改变
（更大步数下 RL 能否学到超越随机的选取）。原始 JSON：
`/tmp/ppi_ms20_{rl,rnd}_s{42,111,123}.json`。

### 10.1 结果表（test 指标，best checkpoint by valMacAUC）

**rl（RL，`--max-steps 20`）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 6477 | 5 | 0.7523 | 0.7606 | 0.7960 | 0.4768 | 0.5270 |
| 111 | 7010 | 17 | 0.7794 | 0.7823 | 0.8048 | 0.5880 | 0.6125 |
| 123 | 6504 | 19 | 0.7889 | 0.7926 | 0.8097 | 0.5804 | 0.6040 |
| 均值±std | 6664±301 | — | 0.7735±0.0190 | 0.7785±0.0163 | 0.8035±0.0070 | 0.5484±0.0621 | 0.5812±0.0471 |

**rnd（random-subset，19..21 节点，恒含 u/v）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 203 | 20 | 0.7783 | 0.7824 | 0.8131 | 0.5847 | 0.6306 |
| 111 | 203 | 20 | 0.7824 | 0.7858 | 0.8062 | 0.5689 | 0.6050 |
| 123 | 204 | 18 | 0.7815 | 0.7806 | 0.8074 | 0.5591 | 0.5966 |
| 均值±std | 203±0.5 | — | 0.7807±0.0021 | 0.7829±0.0027 | 0.8089±0.0037 | 0.5709±0.0129 | 0.6107±0.0177 |

### 10.2 配对 diff（rl − rnd，括号内为正向 seed 数）

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| rl − rnd | −0.0072±0.0171（1/3） | −0.0045±0.0170（1/3） | −0.0054±0.0103（1/3） | −0.0225±0.0740（2/3） | −0.0296±0.0641（2/3） | +6460±301 |

### 10.3 规模对齐与诊断

- **RL ms20 每轨迹 18.13 步（三 seed 完全一致）→ 最终图规模均值 20.1 节点**。
  轨迹长度与策略无关：`k_hops=1` 下 frontier ≈ 区域−已选，动作直到区域耗尽或
  `max_steps` 截断；1-hop 区域均值 45.6 节点（§8.2），打满区域需 ~44 步，故
  ms20 对多数目标构成截断（对照 ms5 的 4.96 步同样为截断值）。
- **rnd 取 19..21（均值 20）对齐 RL 的 20.1**，同 §8.3 的 6..7 对齐 6.96 的
  做法；区域均值 45.6，多数目标可容纳目标规模。
- 速度：rnd 总 203 s（≈ ms5 rnd 的 2×，图更大但无动作）；RL ms20 总 6664 s，
  **≈ rnd 的 33×、≈ RL ms5（1497 s）的 4.5×**（step 图前向随步数线性增长）。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

### 10.4 跨预算对比（同环境或标注历史）

| 条件 | 节点规模 | MacAUC | MacF1 | 秒 | 环境 |
|---|---:|---:|---:|---:|---|
| static（§8.2） | ~45.6 | 0.8047±0.0046 | 0.5947±0.0123 | 337 | 历史（2025-08-25） |
| rnd ms20（本次） | ~20 | 0.7829±0.0027 | 0.5709±0.0129 | 203 | 本次环境 |
| rnd ms5（§8.3） | ~6.5 | 0.7586±0.0048 | 0.5146±0.0219 | 101 | 历史（2025-08-25） |
| rl ms20（本次） | ~20.1 | 0.7785±0.0163 | 0.5484±0.0621 | 6664 | 本次环境 |
| rl ms5（本次附带复跑） | ~7 | 0.7787±0.0086 | 0.5532±0.0434 | 1497 | 本次环境 |
| rl ms0（§8.0） | 2 | 0.7497±0.0120 | 0.5045±0.0183 | 87 | 历史（2025-08-25） |

- **RL：ms5 → ms20 基本持平**（MacAUC 0.7787→0.7785、MacF1 0.5532→0.5484、
  valMacAUC 0.7756→0.7735），F1 微降且 seed 方差上升（MacF1 std 0.043→0.062，
  s42 bestEp 5 早峰 MacF1 0.4768）。
- **rnd：7 → 20 节点明显获益**（MacAUC +0.024、MacF1 +0.056；注意 §8.3 为旧
  环境数字，跨环境有 ~±0.005-0.01 漂移风险，方向性结论；本次无同环境 ms5 rnd
  对照）。
- static（~46 节点）仍为全部条件最优（MacAUC 0.8047）。

### 10.5 判读

- **ms20 下 RL 仍与同规模随机持平（或略低）**：配对五项均值全负但均在 seed
  噪声内（MacAUC −0.0045±0.0170、1/3 正；MacF1 −0.0225±0.0740、2/3 正），
  rnd 方差显著更小（MacAUC std 0.0027 vs 0.0163）——**更大预算未让 RL 超越
  随机，§8.3 "策略相对随机增益 ≤ 噪声"结论在 20 节点下成立且更稳**。
- **更多步的预算对 RL 无益、对随机有益**：RL ms20 ≈ RL ms5（持平/微降），而
  随机随上下文量单调上升（7→20 节点 +0.024 MacAUC）→ 上下文量主导结论再次
  成立：RL 选取既未在 7 节点也未在 20 节点上从"更多可用上下文"中获益，
  REINFORCE 的稀疏噪声奖励不足以驱动有效选取策略。
- **§9.5 优先方向 P1a（k_hops=2 + 更多步）的"更多步"一半被证伪方向**：扩大
  max_steps 只增加 4.5× 成本、无指标收益；剩余候选：k_hops=2（改变候选空间
  结构而非预算量），或"全邻域出发剪枝 + Δn 惩罚"式重构（在 static 之上做
  减法，直接面向 readout 的均值池化上下文利用方式）。
- 跨预算表格中 static > rnd20 > rl20 ≈ rl5 ≈ rnd7 > rl0 的排序与"上下文量
  主导、选取策略无增益"的总结论一致（static 为全部条件信息上界）。

## 11. 全图 KG 实验记录（SHS27k bfs k_hops=2 ms5：rl/rnd/static，2025-08-27）

§10 证伪了 P1a 的"更多步"一半；本实验检验另一半 **k_hops=2**（候选空间由
1-hop 区域扩为 2-hop 区域，`max_steps` 维持 5）。配置同 §7/§8（`h128/g0.9/20ep`，
seed 42/111/123，CPU，每进程 3 线程、同条件 3 seed 并行），仅 `--k-hops 2`；
跑全消融三条件：rl（ms5）、rnd（6..7 节点，对齐 RL 的 7 节点）、static（整个
2-hop 区域）。原始 JSON：`/tmp/ppi_k2_{rl,rnd,static}_s{42,111,123}.json`。

### 11.1 区域规模诊断（train 目标 4562 个实测）

| 区域 | 节点 mean/median/max | 诱导边 mean/median/max |
|---|---:|---:|
| k1（§8.2 历史） | 45.6 / 41 / 156 | 238 / 131 / 1451 |
| k2（本次） | 334.5 / 310 / 835 | 2147 / 1854 / 5294 |

2-hop 区域覆盖全图 ~20%（1690 蛋白），比 k1 大 ~7 倍；区域 >200 节点的目标
占 3349/4562。RL k2 ms5 每轨迹 4.996 步（全部打满 5 步上限）→ 最终图规模
均值 ~7 节点，与 k1 相同 → rnd 沿用 6..7 对齐有效。

### 11.2 结果表（test 指标，best checkpoint by valMacAUC）

**rl（RL，`--k-hops 2 --max-steps 5`）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 1658 | 17 | 0.7547 | 0.7583 | 0.7892 | 0.5455 | 0.5889 |
| 111 | 1697 | 13 | 0.7714 | 0.7766 | 0.8074 | 0.5623 | 0.6065 |
| 123 | 1715 | 14 | 0.7795 | 0.7798 | 0.8090 | 0.5683 | 0.6072 |
| 均值±std | 1690±29 | — | 0.7686±0.0127 | 0.7716±0.0116 | 0.8019±0.0110 | 0.5587±0.0119 | 0.6009±0.0103 |

**rnd（random-subset，6..7 节点，2-hop 区域均匀随机）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 108 | 14 | 0.7536 | 0.7522 | 0.7795 | 0.5203 | 0.5546 |
| 111 | 108 | 11 | 0.7744 | 0.7743 | 0.7909 | 0.5620 | 0.5965 |
| 123 | 108 | 11 | 0.7544 | 0.7502 | 0.7805 | 0.5439 | 0.5820 |
| 均值±std | 108±0.3 | — | 0.7608±0.0118 | 0.7589±0.0134 | 0.7836±0.0063 | 0.5421±0.0209 | 0.5777±0.0213 |

**static（整个 2-hop 区域诱导子图，~334 节点）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 2931 | 19 | 0.7837 | 0.7883 | 0.7971 | 0.5675 | 0.5893 |
| 111 | 2932 | 16 | 0.7913 | 0.7876 | 0.8051 | 0.5791 | 0.6038 |
| 123 | 2921 | 6 | 0.7825 | 0.7938 | 0.8151 | 0.5620 | 0.6019 |
| 均值±std | 2928±6 | — | 0.7858±0.0048 | 0.7899±0.0034 | 0.8057±0.0090 | 0.5695±0.0088 | 0.5983±0.0079 |

### 11.3 配对 diff（括号内为正向 seed 数）

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| rl − rnd | +0.0078±0.0152（2/3） | +0.0127±0.0148（3/3） | +0.0182±0.0096（3/3） | +0.0166±0.0142（3/3） | +0.0232±0.0123（3/3） | +1582±29 |
| static − rl | +0.0173±0.0132（3/3） | +0.0183±0.0102（3/3） | +0.0039±0.0054（2/3） | +0.0109±0.0151（2/3） | −0.0025±0.0028（1/3） | +1238±35 |
| static − rnd | +0.0250±0.0071（3/3） | +0.0310±0.0157（3/3） | +0.0221±0.0109（3/3） | +0.0275±0.0171（3/3） | +0.0206±0.0137（3/3） | +2820±6 |

跨 k 对比（RL/rnd 与同日同环境 k1 ms5 配对；static 与 §8.2 历史，方向性）：

| 对比 | MacAUC | MacF1 | 说明 |
|---|---:|---:|---|
| rl k2 − rl k1 | −0.0071±0.0171（1/3） | +0.0055±0.0454（2/3） | k2 区域对 RL 自身无增益，+13% 时间 |
| rnd k2 − rnd k1（历史 §8.3） | +0.0003（方向性） | +0.0275（方向性） | 随机不因区域扩大获益（6.5 节点太稀疏） |
| static k2 − static k1（历史 §8.2） | −0.0148（方向性） | −0.0252（方向性） | **更大上下文反而降低 static** |

### 11.4 判读

- **RL 首次跑赢同规模随机（k2 下）**：rl−rnd 五项全正、其中 4 项 3/3 正
  （MacAUC +0.0127±0.0148、MacF1 +0.0166±0.0142、MicAUC/MicF1 3/3）——
  §8.3/§10"策略相对随机增益 ≤ 噪声"结论**不适用于大候选空间**：候选区域
  45.6→334.5 节点后，均匀随机大多选到无信息的外围节点，学习选取的价值首次
  显现。幅度与 seed std 同量级（diff ≈ std），3-seed 方向一致，待扩 seed 确认。
- **static 仍是信息上界但优势收窄**：static−rl MacAUC +0.018（k1 时 +0.029、
  MacF1 +0.043 → k2 时 +0.011、MicF1 1/3 正反超）。
- **上下文量非单调**：static k2（334 节点）明显低于 static k1（46 节点）
  （MacAUC −0.015、MacF1 −0.025，跨环境方向性；s123 bestEp 6 早峰提示大图
  过拟合/均值池化稀释）→ 存在上下文量的最优区间，不是越大越好。
- **RL k2 ≈ RL k1**：更大的候选空间没有转化为 RL 自身提升（MacAUC −0.007、
  +13% 时间）；RL 的 5 个动作仍主要在 1-ring 内取值（初始 frontier 即 1-ring），
  2-ring 信息未被利用。
- 综合：k_hops=2 的价值不在"给 RL 更多上下文"，而在**放大了选取策略的
  区分度**（候选空间大 → 选取质量可分离）；但整体最优仍是 static k1（46
  节点、MacAUC 0.8047）。若想利用"选取在大空间有正价值"，方向是让 RL 在
  static k1 邻域之上做**减法式剪枝**（候选 = 46 节点区域、从中选 7-20 节点，
  与 §9.5 P1b 重合），或扩 seed 确认 rl−rnd 的 k2 正增益后再考虑 k2 部署。
- 速度：rl 1690 s（k1 的 1.13×）、static 2928 s（k1 的 8.7×，区域 ~7 倍 +
  O(n²) 诱导边）、rnd 108 s。可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

## 12. 全图 KG 实验记录（P0 启发式选取诊断：heuristic vs random-subset 同预算，2026-09-04）

动机（P0 前置诊断）：固定预算下"有信息的拓扑选取"能否超越随机、逼近 static？
以此决定后续是"修 RL"还是"先修 readout/预算"。新增零参数
`HeuristicSampler`（`--sampler heuristic`，提交与 rnd/static 同款协议）：
候选空间与 static/random-subset 完全一致（`_k_hop_region` ∩ 安全邻接），
确定性拓扑排序——u/v **公共邻居** → 单侧邻接 → 区域其余，层内按安全度数
降序、id 升序；预算机制与 random-subset 完全一致（同一
`--random-subset-min/max-size` 均匀抽样），故 heur − rnd 为同环境、同预算的
纯策略效应。只用拓扑、无任何标签输入。

配置同 §7/§8（`h128/g0.9/20ep`，seed 42/111/123，CPU，每进程 3 线程、同条件
3 seed 并行），`--split bfs`。原始 JSON：
`/tmp/ppi_{heur,rnd}{7,20}_s{42,111,123}.json`。

### 12.1 结果表（test 指标，best checkpoint by valMacAUC）

**heur7（heuristic，6..7 节点）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 113 | 20 | 0.7799 | 0.7836 | 0.8066 | 0.5774 | 0.6019 |
| 111 | 115 | 18 | 0.7892 | 0.7891 | 0.8123 | 0.5927 | 0.6234 |
| 123 | 113 | 3 | 0.7821 | 0.7797 | 0.7937 | 0.5477 | 0.5653 |
| 均值±std | 114±1 | — | 0.7837±0.0040 | 0.7841±0.0038 | 0.8042±0.0078 | 0.5726±0.0187 | 0.5969±0.0240 |

**rnd7（random-subset，6..7 节点，同环境对照）**：

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 108 | 14 | 0.7595 | 0.7640 | 0.7893 | 0.5336 | 0.5685 |
| 111 | 105 | 6 | 0.7518 | 0.7550 | 0.7843 | 0.4908 | 0.5461 |
| 123 | 107 | 20 | 0.7625 | 0.7569 | 0.7770 | 0.5196 | 0.5494 |
| 均值±std | 107±2 | — | 0.7579±0.0045 | 0.7586±0.0039 | 0.7835±0.0051 | 0.5146±0.0178 | 0.5547±0.0099 |

**heur20 / rnd20（heuristic / random-subset，19..21 节点）**：

| 条件 | 秒 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|
| heur20 | 183±1 | 0.7810±0.0064 | 0.7840±0.0058 | 0.8071±0.0039 | 0.5534±0.0254 | 0.5865±0.0206 |
| rnd20 | 161±1 | 0.7807±0.0017 | 0.7829±0.0022 | 0.8089±0.0030 | 0.5709±0.0106 | 0.6107±0.0144 |

### 12.2 配对 diff（括号内为正向 seed 数）

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---|---|---|---|---|
| heur7 − rnd7 | +0.0258±0.0083（3/3） | +0.0255±0.0062（3/3） | +0.0207±0.0052（3/3） | +0.0579±0.0318（3/3） | +0.0422±0.0258（3/3） |
| heur20 − rnd20 | +0.0003±0.0065（2/3） | +0.0011±0.0079（2/3） | −0.0018±0.0066（2/3） | −0.0175±0.0345（2/3） | −0.0243±0.0340（1/3） |

环境一致性：rnd7（0.7586±0.0039）与 §8.3 历史（0.7586±0.0048）、rnd20
（0.7829±0.0022）与 §10（0.7829±0.0027）几乎完全一致 → 本次环境无漂移，
历史 static/RL 数字可直接作参照。

### 12.3 选取行为（train 4562 目标实测）

- 区域规模 mean 45.5 / median 41 / max 156（与 §8.2 的 45.6 一致）；
- 68.6% 目标的 1-hop 区域内存在 ≥1 个公共邻居；
- 预算（均值 6.5 节点）落层：common 42.1% / touching 57.3% / rest 0.6%
  （k=1 区域内几乎所有候选都邻接目标，rest 基本只有 proxy 邻居）→
  heuristic 的有效成分 = **公共邻居优先**。

### 12.4 判读

- **预算内选取结构存在**：7 节点下 heuristic 五项全 3/3 正超随机（MacAUC
  +0.026、MacF1 +0.058），且 MacAUC 0.7841±0.0038 略超同环境 RL ms5（§10
  复跑 0.7787±0.0086）——一个零参数拓扑规则（共邻优先）就达到/超过了学习
  策略的水平。P0 的"该预算下无结构可学"假设被拒绝 → **RL 有真实可学空间，
  P2（给策略补结构特征，尤其共邻/度数指示）是第一杠杆**：当前策略表示
  （state=已选均值、candidate=自身 embedding）看不到邻接结构，故学不到
  这条规则。
- **但优势随预算消失**：20 节点下 heur ≈ rnd（差 ≈0、F1 微负）——公共邻居
  配完后（约前 3-4 个选取），排序在 7-20 节点段无附加值，剩余选取与随机
  无异；两种规则一起收敛到 ~0.783 平台。
- **选取天花板仍低于 static**：heur20/rnd20（0.783）< static k1（46 节点，
  0.8047，历史环境）——20→46 节点的上下文量收益（~+0.02）真实存在，
  "上下文量主导"结论不变；同时 heuristic 平台说明该收益主要藏在
  "static 区域中排序靠后的那 26 个节点"里，这正是 P1（从 static 区域学习
  排序/剪枝）的目标空间。
- 后续优先级修正：**P2 ≥ P1 > P3**。RL 至少应学会"共邻优先"（P2 补结构
  特征后可直接验证）；P1 的学习排序若能把 20-30 节点配到接近 static 水平
  即净收益 + 省时；P3（readout）逻辑不变——20→46 仍有收益说明均值池化在
  46 节点未饱和，readout 改造用于抬高上限。
- 速度：heur7 114s、heur20 183s（与 rnd 同量级，≈ RL ms5 的 1/13）。heur
  建议长期保留为 RL 的**行为基准**：任何 RL 改动先在同预算下与 heur 配对
  比较，打不过 heur 的策略改动视为无效。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

## 13. 全图 KG 实验记录（P2a：RL 结构特征 + 启发式先验初始化，2026-09-04）

动机：§12（P0）证明 7 节点预算内选取结构存在（共邻优先零参数规则超随机
+0.026 MacAUC、≥ RL ms5）。P2a 检验：**把该规则作为 RL 策略的先验初始点**
并补齐候选拓扑特征后，REINFORCE 能否保持并超越 heuristic。

实现（`--sampler-structural-features`，RL 专属）：

- 8 维候选拓扑特征 `φ_topo`：共邻指示、触目标数、归一化安全度数
  （log1p(d)/log1p(d_max)）、与已选集连接率、到 u/v 的有界 hop 距离
  （cap 2·k_hops）、对 u/v 的 Adamic-Adar；全部经目标安全邻接定义；
- 注入路径：零初始化 `Linear(8→hidden)` 进 candidate 表示（学习路径，起始
  no-op）+ 可训练线性 skip `scores += w·φ_topo`，`w` 初始化 `(8,4,1,0,…)`——
  greedy argmax 精确复现 heuristic 词典序（共邻≫单侧≫其余，层内按度数），
  softmax 采样保留 e^-8 探索下限；
- 奖励/RTG/交替协议不变；零初始化 + `fork_rng` 保持 seeded 配对（73 项单测
  覆盖特征手算值、先验行为、梯度、入参守卫）。

配置同 §7/§12（`h128/g0.9/20ep/ms5/k1`，seed 42/111/123，CPU，每进程 3 线程、
3 seed 并行），`--checkpoint-dir` 留存最优。原始 JSON：
`/tmp/ppi_p2a_s{42,111,123}.json`。

### 13.1 结果表（test 指标，best checkpoint by valMacAUC）

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 1607 | 18 | 0.7830 | 0.7840 | 0.8055 | 0.5733 | 0.6174 |
| 111 | 1584 | 17 | 0.7624 | 0.7597 | 0.7900 | 0.5580 | 0.5918 |
| 123 | 1610 | 6 | 0.7684 | 0.7710 | 0.7897 | 0.5000 | 0.5321 |
| 均值±std | 1600±16 | — | 0.7713±0.0086 | 0.7716±0.0099 | 0.7951±0.0074 | 0.5438±0.0315 | 0.5804±0.0357 |

### 13.2 配对 diff（同环境，括号内为正向 seed 数）

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 | 秒 |
|---|---|---|---|---|---|---:|
| P2a − heur7 | −0.0125±0.0122（1/3） | −0.0126±0.0124（1/3） | −0.0091±0.0093（0/3） | −0.0288±0.0182（0/3） | −0.0164±0.0226（1/3） | +1486±16 |
| 参照：RL ms5（§10 复跑，2025-08-27 环境） | — | 0.7787±0.0086 | — | 0.5532±0.0434 | — | 1497 |

P2a 与 RL ms5 基线在噪声内持平（MacAUC 差 −0.007 < 跨环境漂移
±0.005-0.01，方向性）：**结构特征 + 先验未改变 RL 水平**。

### 13.3 行为检查（各 seed 最优 checkpoint，greedy，415 个 train 目标）

| seed | 首选共邻命中率 rl / heur | 前 3 选共邻占比（rl） | 与 heur 选取 Jaccard |
|---|---|---|---|
| 42 | 0.711 / 0.716 | 0.530 | 0.415 |
| 111 | 0.716 / 0.716 | 0.531 | 0.397 |
| 123 | 0.716 / 0.716 | 0.531 | 0.375 |

首选命中率 0.714 ≈ 0.716（后者即"区域内存在共邻的目标占比"68.6% 的上限）
→ **先验行为在第 1 步被完整保留**；Jaccard 0.40 → 第 2-5 步选取偏离
heuristic 且未收敛到更优规则。

### 13.4 判读（对照 §13 预注册验收标准）

- (i) MacAUC ≥ heur7：**未通过**（配对 −0.0126，1/3 正）；(iii) >0.79：未
  发生；(ii) 行为：先验在第 1 步保留、后续步漂移。
- **核心结论：策略特征空间不是瓶颈**。即使策略从已知良好规则（greedy 初始
  ≡ heuristic）出发且第 1 步行为被保留，REINFORCE 训练仍把第 2-5 步更新到
  劣于固定规则的水平（P2a < heur7 配对成立），与普通 RL 持平——约束在
  **奖励信号**（冻结中更新 predictor 的单样本 BCE 差分：噪声大、目标非平稳、
  逐步 credit assignment 弱）。
- 优先级修正（覆盖 §12.4 的 P2 ≥ P1 > P3）：**P4（奖励机制：EMA predictor
  算奖励、每目标重复采样平均、奖励方差治理）与 P1（剪枝式重构——动作语义
  改为"从 static 区域删除"，弱化逐步 credit assignment）上升**；P2 的纯特征
  路线（σ/τ/ρ 关系块）降级——奖励机制修复前，继续给策略加特征预期无效。
- heur 依然是无学习上界下的行为基准：任何 sampler 改动先同预算配对 heur7。
- 速度：1600±16 s（≈ RL ms5 的 1.07×，φ_topo 的 Python 开销 ~7%）。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

## 14. 全图 KG 实验记录（R1：margin 奖励，2026-09-04）

动机：§13（P2a）把瓶颈定位在奖励信号。R1 检验 RISE-DDI 式奖励重设计：
标签对齐平均概率边际 `M(p)=mean_j((2y_j−1)·p_j) ∈ [−1,1]`，
`r_t = (Δ_t>0 ? w⁺ : w⁻)·Δ_t`，`Δ_t = M(step graph) − M(G_0)`——**固定 G0
参考**（非逐步 BCE 差分）、非对称缩放（`w⁺:w⁻ = 2:1`）。实现
`--reward-margin`（默认关闭保持历史可复现），`mean_final_margin` 进入
epoch 记录（两种模式均输出）；78 项单测覆盖边际计算、方向缩放、聚合与入参。

配置同 §7（rl ms5/`h128/g0.9/20ep`，seed 42/111/123，CPU，每进程 3 线程、
3 seed 并行）。原始 JSON：`/tmp/ppi_r1_s{42,111,123}.json`。

### 14.1 结果表（test 指标，best checkpoint by valMacAUC）

| seed | 秒 | bestEp | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 42 | 1445 | 10 | 0.7787 | 0.7872 | 0.8064 | 0.5660 | 0.5917 |
| 111 | 1410 | 20 | 0.7745 | 0.7735 | 0.7939 | 0.5333 | 0.5597 |
| 123 | 1427 | 18 | 0.7782 | 0.7773 | 0.8058 | 0.5335 | 0.5829 |
| 均值±std | 1427±18 | — | 0.7771±0.0019 | 0.7794±0.0058 | 0.8020±0.0057 | 0.5443±0.0154 | 0.5781±0.0135 |

### 14.2 配对 diff（同环境，括号内为正向 seed 数）

| 对比 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---|---|---|---|---|
| R1 − heur7 | −0.0066±0.0058（0/3） | −0.0048±0.0080（1/3） | −0.0022±0.0125（1/3） | −0.0283±0.0220（0/3） | −0.0188±0.0337（1/3） |
| 参照：R0 = RL ms5（§10 复跑） | — | 0.7787±0.0086 | — | 0.5532±0.0434 | — |
| 参照：P2a（§13） | — | 0.7716±0.0099 | — | 0.5438±0.0315 | — |

R1 与 R0 在噪声内持平（MacAUC +0.0007），F1 微降；valMacAUC 的 seed 间
std 由 0.0086 降至 0.0019（**训练稳定性显著提升**）。

### 14.3 margin 曲线（验收标准 iii）

三 seed 的 `mean_final_margin` 全部**单调上升**：~0.00 → 0.26（epoch 20），
`mean_reward` 同步上升后趋平（例如 s42：−0.006 → 0.266）。策略完全学会了
优化新目标。

### 14.4 判读（对照 §13 预注册验收标准）

- (iii) 边际曲线单调：**通过**（3/3）；(i) R1 ≥ heur7：**未通过**（MacAUC
  −0.005、MacF1 −0.028），与 R0 持平。
- **核心判读：奖励重设计机械上成功、指标上中性**——"超越 G0 的边际"可以被
  稳定学习（且训练方差降 4×），但边际提升不等于"找到更好的上下文"：
  policy/predictor 共适应可以在训练目标上持续得手而不改变测试水平。这与
  "均值池化读出下 ~7 节点上下文存在 ~0.78 平台"（§8.3/§10/§12）完全一致。
- **sampler 侧两条修复路线（P2a 特征、R1 奖励）至此均被验证为不充分**，
  优先级最终收敛：**A1（predictor 读出重构：target 锚定注意力 + PPR 位置
  编码，即 RISE-DDI 的 LinkAttention 路线）→ A2（采样语义：static 底座 ∪
  RL 增补）**。在 A1 落地前，任何 sampler 侧改动预期都无法突破 0.78 平台。
- R1 建议保留为默认配置候选：指标中性、训练更稳、margin 诊断免费；后续
  RL 实验建议默认带 `--reward-margin` 并沿用 heur7 同预算配对判定。
- 速度：1427±18 s（≈ R0，margin 计算开销可忽略）。
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

## 15. 全图 KG 实验记录（A1：读出重构——target 锚定注意力 + PPR 位置编码，2026-09-04）

动机：§13（P2a 特征）与 §14（R1 奖励）证明 sampler 侧修复均无法突破
~0.78 平台，瓶颈收敛到 predictor 读出（均值池化稀释：§8.2/§11/§12）。
A1 按 RISE-DDI（AAAI'25）读出机制重构：**保留 mean-pool 并加性追加**
目标锚定 LinkAttention 摘要 `z`——e1/e2 作为独立查询、GATv2 式门控、
PPR 位置编码拼进 key、共邻/单侧邻接层各用独立编码器、其余节点 PE=0；
输出宽 3h→4h（`--readout attention`，默认 mean 保持历史可复现）。
PPR 在无标签全图拓扑上 forward-push 预计算（`src/ppr.py`，α=0.15、
eps=5e-6，按目标惰性缓存）。86 项单测覆盖 PPR 精度（幂迭代参照）、
层级划分、注意力形状/梯度/多图 batch、入参守卫。

配置：SHS27k bfs / `h128/g0.9/20ep`，seed 42/111/123，CPU，每进程 3 线程、
同条件 3 seed 并行。原始 JSON：
`/tmp/ppi_{v2,v3h,v1sk1,v1sk2}_s{42,111,123}.json`。

### 15.1 结果表（test 指标，best checkpoint by valMacAUC）

| 条件 | 秒 | valMacAUC | MacAUC | MicAUC | MacF1 | MicF1 |
|---|---:|---:|---:|---:|---:|---:|
| V2：rl ms5 + margin + attention | 1364±24 | 0.7779±0.0078 | 0.7757±0.0098 | 0.7984±0.0122 | 0.5343±0.0394 | 0.5860±0.0322 |
| V3：heur 6..7 + attention | 262±0 | 0.7835±0.0023 | 0.7836±0.0056 | 0.8081±0.0023 | 0.5460±0.0094 | 0.5830±0.0040 |
| **V1a：static k1 + attention** | 599±3 | 0.8047±0.0114 | **0.8105±0.0114** | **0.8322±0.0108** | **0.6158±0.0156** | **0.6454±0.0236** |
| V1b：static k2 + attention | 4038±20 | 0.8055±0.0042 | 0.8065±0.0053 | 0.8138±0.0108 | 0.5767±0.0219 | 0.5934±0.0249 |

参照（均含于前文）：static k1 mean-pool（§8.2，0.8047/0.5947）、R1（§14，
0.7794/0.5443）、heur7（§12，0.7841/0.5726）。

### 15.2 关键对比

- **V1a − static k1 mean-pool（§8.2 历史，方向性）**：MacAUC +0.006
  （2/3 正：+0.003/+0.018/−0.004）、**MacF1 +0.021（3/3 正）**、
  MicAUC +0.012、MicF1 +0.027——**全面超越历史最优，新项目最优
  MacAUC 0.8105 / MacF1 0.6158**；
- V2 − R1：−0.004（1/3）；V2 − heur7：−0.008（1/3）——7 节点预算下 RL
  换 attention 读出无变化；
- V3 − heur7：MacAUC −0.0005、MicAUC +0.004、MacF1 −0.027——小预算下
  heur 同样无增益；
- **稀释测试（attention k2 − attention k1）**：MacAUC −0.004、MacF1
  −0.039、MicF1 −0.052——k2 仍劣于 k1（对照 mean-pool 时代 k2−k1 =
  −0.015/−0.025），**稀释未被注意力根除**。

### 15.3 判读

- **天花板被抬高**：static k1 + attention 全面超越历史全部配置（四项主
  指标 0.8105 / 0.8322 / 0.6158 / 0.6454），读出假说获证——**上下文充足
  时，target 锚定注意力读出显著优于均值池化**；
- **小预算平台不动**：RL/heur 在 6.5–7 节点下换 attention 读出无增益
  （V2≈R1、V3 MacF1 微降）——0.78 平台是"小上下文预算"的属性而非读出
  问题，进一步确认"上下文量主导"；
- **稀释未根除**：static k2 仍劣于 k1（F1 差距甚至更大）——加性设计保留
  的 mean-pool 项与 334 节点图的噪声可能仍是拖累，上下文最优区间仍在 k1
  （~46 节点）附近；
- **RL 缺口反而扩大**：V1a − V2 = +0.035 MacAUC——RL 选取的边际价值问题
  依旧。sampler 侧唯一未测路径是 **A2（static 底座 ∪ RL 增补）**：在
  attention 读出下增补节点不再被均值池化稀释，可对其作公平检验；
- 速度：attention 开销与图规模成正比（heur 262s vs 114s、static k1 599s
  vs 337s、static k2 4038s vs 2928s）；最优配置 V1a 599s 仍远快于任何
  RL 运行（≈ ms5 的 2/5）；
- 顺带修复既有 bug：非 RL sampler + `--checkpoint-dir` 保存时
  `sampler_optimizer=None` 崩溃；
- 可见性分组计数不变（BS 0 / ES 1078 / NS 460）。

## 16. 验证

```bash
python -m unittest discover -s tests -v
python -m compileall -q src tests
git diff --check
```
